import React from 'react';

import './blogs-preview.styles';

const BlogsPreview = () => (
  <div className='blogs-preview'>
    {
      // TODO - display x-amount of blogs with pagination possibly
      // TODO - render a <Blog /> for each item of blog list
    }
    BLOGS PREVIEW
  </div>
);

export default BlogsPreview;